services:
  nextjs:
    build: .
    container_name: nextjs
  
  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - "3003:3003"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - nextjs